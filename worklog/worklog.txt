===========
  COURS 1
===========

Mission : piloter le matériel
Instruction Set Architecture pour pouvoir communiquer avec le hardware depuis notre appli C.

Le câble peut contrôler/débugguer le matériel et faire des I/O via terminal (ligne série).
Qemu exécute des programmes (dont le debugguer) comme si c'était sur du vrai matériel.
- le configurer pour avoir une board reliée à un terminal via un câble, le terminal est une fenêtre de notre ordi

arm-none-eabi-gcc est capable de créer du code qui s'exécute sur la machine target.
Qemu donne la possibilité de travailler sur du hardware que nous n'avons pas, son gdb-server permet de débugguer, accédé par un
autre terminal où on débuggue avec arm-none-eabi-gdb <fichier>.elf et qui n'est pas connu par le matériel.

Fichier .elf => production de la compilation, relogeable, le linker détermine où chaque section est chargée en mémoire
- le script du linker peut être craché par gcc je crois, mais pour le projet il y a kernel.ld
- Qemu s'occupe de charger les sections en mémoire

Processeur écrit/lit dans la mémoire en fetch/decode/execute, local interconnect véhicule le load/store vers :
- controleurs de mémoire Flash/EPROM se rappelle du contenu, et DDR ne se rappelle de rien
- controller UART pour la ligne série, avec le terminal relié au bout
- zones mémoire pour parler avec les autres périphériques (MMIO registers)
Les registres contiennent les valeurs à load/store.

Quelles sont les zones de la mémoire du hardware ?
- voir la doc
- dans notre cas, l'adresse 0 est le BIOS (mappé) qui charge notre programme directement (car pas d'OS) à l'adresse 0
(c'est en fait un reset handler, qui ensuite saute vers le boot code en assembleur "startup.s" qui saute vers la première instruction de notre programme)
	- le gcc frame pointer sert à gdb de savoir où est la fin de la pile pour parcourir les appels de fonctions
- Qemu se charge de placer le programme au bon endroit

Qemu peut décrire l'illusion créée pour le matériel si l'émulation n'est pas exactement comme dans la doc
- comprendre les options données à Qemu, demander rapidement au prof car "c'est l'enfer"


===========
  WEEK 1
===========

DONE tout installer, lancer sur Qemu avec terminal attaché
DONE faire marcher l'UART0 pour faire une echo console

Si bloqué lors du make, Ctrl-A + C et quit pour quitter Qemu.
J'ai des erreurs qui commencent par "ALSA lib confmisc.c:855:(parse_card) cannot find card '0'", je peux faire avec ?
>> tant que ça ne bloque pas oui

- l'ISA contient le jeu d'instructions assembleur

- quelle est la board qu'on émule ? C'est quoi versatile AB ou PB ?
>> CPU est cortex-a8, le reste de la board est "versatile" et contient ça :
https://www.qemu.org/docs/master/system/arm/versatile.html
>> les slides parlent de versatile pb, alors que versatile ab dans Makefile

- où est-ce qu'on donne le terminal dans le Makefile ?
>> -serial mon:stdio dit que le terminal est la fenêtre où se lance Qemu

- c'est quoi kernel.ld ? kernel.bin ?
>> kernel.ld est un linker script, kernel.bin est une copie du binaire ELF, je ne sais pas à quoi elle sert
>> kernel.elf a les descriptions, infos autre que code et données, .bin contient ce qui est projeté en mémoire (y compris les trous si .elf a des trous)
DONE comprendre ce que kernel.ld a choisi comme setup

- qu'est-ce que Zync-7000 ?
>> la SoC
- qu'est-ce que DDR ?
>> Double Data Rate memory
- qu'est-ce qu'OCM ?
>> On-Chip Memory, parce qu'il y a de la mémoire là-dedans, mappé aux adresses les plus hautes, contient le "BIOS"
- on initialise le contrôleur de mémoire (qui ?) et on copie le "BIOS" dedans pour l'exécuter
- Qemu charge les sections de notre .elf (relogeable) à partir de l'adresse 0 (écrase le "BIOS")
- le code de exception.s est donc exécuté pour mettre en place le vecteur d'interruption
- le reset handler défini dans startup.s est exécuté (comprendre pourquoi), les explications sont déjà dans le code
- la fonction _start est appelée
- on vérifie que la stack soit de taille positive
	- qu'est-ce que "MEMORY" ?
	>> vient sûrement du Makefile
- on initialise les UARTS avec les bar données dans la doc DUI0225 (versatile board)
- qu'est-ce que les registres MMIO ?
>> ce qui lit ou écrit des données dans les zones réservées au devices
- utiliser les registres MMIO pour lire le registre UART_FR et lire/écrire dans UART_DR
>> flag RXFE et TXFF pour attendre qu'un octet soit disponible en lecture ou puisse être écrit sans être perdu

- où est le code des handlers autre que reset ? A implémenter les prochaines séances ?
>> peut-être dans step2 oui


===========
  COURS 2
===========

Mission : éliminer le spinning (attente active de pression sur bouton ou touche clavier).

Il faut vite s'occuper du matériel, ex: traiter les caractères reçus par l'UART pendant que l'utilisateur tape une phrase
Le processeur peut exécuter une instruction pour s'endormir si rien à faire, et sera réveillé par un signal eléctrique.
VIC (device) est branché aux devices et fait le coordinateur, il est appelé par les controllers et appelle le CPU.
VIC a une mémoire pour ne pas perdre les demandes d'interruption en attente de traitement.
Processeur autorise de sonner à la porte (fil à 5V), autres autorisations pour le VIC et le controller de device (voir plus loin).

Quel numéro d'interruption pour quel device ?
Quelle est la tête du vecteur d'interruption ?
>> voir la doc

Quand le processeur doit réagir aux interruptions ? Comment ?
>> jamais au milieu d'une instruction, mais une ligne en C peut être coupée par une interruption
>> passer en mode interruption, la plupart des registres seront les mêmes
	- sauvegarder sur notre stack en utilisant le lr (le pc est déjà fait dans R14, et cpsr)
	- les interruptions sont désactivées
>> passer le pc en 0x18 pour traiter l'interruption (_isr, Interrupt Service Routine)
	- '^' restaure le mode original
Le processeur a un pipeline a 3 niveaux, donc le pc est en avance par rapport à la prochaine instruction a exécuter

DONE ajouter une 2è stack dans linker script et décommenter la stack IRQ dans check_stack()
"Ne pas laisser de traces du traitement de l'interruption"

Comment donner a UART le droit de lever inter ?
>> utiliser le Summary of registers de la doc que je connais
>> interrupt UARTIMSC mask pour activer les interruptions que en réception, donc bit 4 puis bit 5 plus tard
	- donc le send ne changera pas au début
UARTRIS = ce que je voudrais si je pouvais
UARTMIS = ce que je fais ce qui m'est permis, à traiter
UARTICR = set bit à 1 pour le mettre à 0 dans le UARTRIS, qui entraine UARTMIS
UARTIFLS = pour laisser ou non le FIFO se remplir (envoyer suffisament d'octets pour délencher une interruption)
>> on peut lire les registres avec le débugguer et les modifier à la main
L'interruption est lancée lors de la transition et pas en fonction de la valeur.

Comment dire au VIC de preter attention ?
>> doc section "Programmer's model" comme d'hab (PL190)
VICIRQSTATUS = quelles interruptions sont levées, comme UARTMIS
VICRAWINTR = comme UARTRIS
VICINTENABLE = comme UARTMISC
VICINTENCLEAR = comme UARTICR

CPU :
Fourni, voir irq.s, appelé par fonctions dans isr.c

DONE coder le interrupt handler (à copier depuis les slides)
Tableau de handlers, un par interruption, contient un pointeur vers le traitant, cookie pour savoir où sont les données
Demander au VIC le champ de bits, puis boucle avec un masque pour savoir quels traitants appeler.
Pas d'ack au niveau du VIC.
Slide 32 a l'overview du soft


===========
  WEEK 2
===========

Activation interruptions pour le CPU :
- fourni (lire irq.s)
- code appelé par core_enable_irqs(), à appeler dans _start

Activation interruptions pour le VIC :
- renseigné les adresses vers les registres du VIC
- le registre VICIRQSTATUS a 32 bits, chacun correspondant à un device (voir Figure 17.1 de ARM926EJ-S)
- il faudra appeler les handlers en fonction de quels bits sont levés
- registres VIC manipulés dans isr.c
- l'argument du callback est le numéro d'interruption, je ne sais pas à quoi il sert

Bon, coder ne vaut pas de points si je ne comprends rien.

Mise en place des interruptions pour l'echo console :
- UART : mettre des bits à 1 dans des registres pour permettre d'envoyer des signaux électriques au VIC
- VIC : mettre des bits à 1 dans des registres pour permettre d'envoyer des signaux électriques au CPU
- CPU : registre CPSR et son bit CPSR_IRQ_FLAG à clear pour activer les interruptions

Chaîne des interruptions pour l'echo console :
- on appuie sur le bouton du clavier, receive interrupt du UART est levé (hardware, bit levé)
- avoir le bit levé envoie un signal électrique au VIC
- le VIC a le bit correspondant au fil électrique levé par le signal de l'UART
- avoir le bit levé envoie un signal électrique au CPU
- _isr_handler est appelé
- le CPU passe en mode interruption
	- QUESTION est-ce que _irqs_setup est à compléter ? quand est-ce qu'on enregistre dans irq stack ? Vraiment bloqué sur ce passage là...
	>> A VERIFIER ! Je l'ai complété avec sauvegarde registres + call isr C + restoration registres, logique pour moi
- l'isr lit le masque d'interruptions du VIC (chercher à la bonne adresse mémoire) et appelle les handlers software
- le handler correspondant à l'UART0 regarde le bit mask interruption pour savoir quelle interruption doit être traitée (réception pour nous)
- en return de l'isr le CPU repasse en mode noyau et continue son exécution

UART receive interrupt handler :
Lancé à partir d'un certain seuil de remplissage, donc il faut vider la FIFO de réception dans le handler
Pourquoi l'interruption est envoyée dès le premier caractère dans mon cas ? Dépasse déjà le seuil de remplissage ?
>> on dépasse déjà le seuil, on a un registre pour le seuil dans l'UART, Qemu décide un peu aussi

Normalement il faut faire des ACK pour permettre de relancer la même interruption.
Il n'y a pas besoin au niveau du VIC PL0190 sur ARM car il voit la demande d'interruption des devices en continu,
donc l'ACK au niveau du device va entraîner l'ACK au niveau du VIC.
Il n'y a pas besoin au niveau de l'UART non plus car l'accès mémoire au registre UARTDR semble entrainer un ACK, au moins pour le receive interrupt.


===========
  COURS 3
===========

Où est la race condition ?
Pas de traitements longs ni de synchro dans le traitant (voir le matériel, passer les données dans monde logiciel de boucle principale,
qui fait le plus gros du traitement)
=> du coup on peut être interrompu en faisant des choses dans le boucle principale

Pour passer des octets d'un thread à l'autre sans lock, ring/circular buffer, si machine qui ordonne W et écrit pile comme dans slide
Traitant (ISR) receive produce dans ring, boucle principale consomme.
>> utiliser que ça pour transférer des octets
Mettre les rings dans le cookie

Le core_halt() a une instruction atomique qui lève les interruptions en s'endormant.

Si le ring overflow, il faut panic() et se relancer
NOTE : utiliser un panic() dans notre programme peut servir à remonter la cause d'un crash au débugger
On peut copier les octets des ring buffers au buffers plus grands de l'application pour libérer l'application de cette gestion.
Une meilleure solution est de notifier l'application qu'il y a des octets à lire/écrire

COMMENT METTRE EN PLACE LE DEAL DE LISTENERS (conception)
>> uart_init() sert à donner les fonctions que l'isr doit appeler
>> réception : l'isr appelle quand on reçoit une interruption hardware, c'est seulement là que l'app utilise uart_read/uart_write()
>> si l'app ne lit pas tous les octets, elle ne sera pas rappellée pour lire les octets restants
>> émission : l'isr appelle quand il y a de la place dans le transmit ring, refuse quand il n'y a plus de place

TODO modifier le start pour appeler les listeners en fonction de ce qui se passe dans l'UART

Réception -> on fait rien on attend
Envoie -> remplir la FIFO au delà du seuil


===========
  WEEK 3
===========

More work for week 2, some details/question added.

Comprendre où est la race condition :
- on ne veut pas d'attente active dans le programme principal
- (I'M HERE) tout mettre dans le handler est trop lent si on peut attendre des octets à envoyer par exemple, ça ralentit aussi les autres interruptions derrière
- laisser le handler traiter au niveau du device et déplacer le reste du traitement dans le programme principal
>> requiert un moyen de passer les données au logiciel
>> la race condition vient du fait qu'on peut se faire interrompre par le handler dans le programme principal
- désactiver les interruptions lors des accès à la liste
Qu'est-ce qui se passe de mal pendant la race condition ?
	- si on a une boucle jusqu'à ce que la liste soit vide, on n'oubliera pas le caractère qui a été ajouté pendant le traitement du précédent ?
	- si on ajoute à la fin et on prend au début, les caractères seront toujours traités dans le bon ordre ?
>> ça dépend manip queue
- encore mieux, utiliser un "lock-free (implém ultra précise) ring buffer" qui n'a pas besoin de désactiver les interruptions

Pas encore compris la slide 13.

A quoi sert les listeners et les évènements dans le cas de l'UART ?
>> car le ring buffer peut déborder si on n'extrait pas les octets assez rapidement dans le main loop
Comment ça se passe lors de l'exécution de l'application avec des listeners ?
>> à la fin l'application ne fera que réagir à des évènements avec ses listeners
Comment gérer les évènements si il y en a plusieurs en attente ?
>> le rôle de Mr.Event !

L'implémentation des listeners :
QUESTION uart_read() et uart_write() dans le listener framework correspond au uart_receive() et uart_send() ?
>> attention à la couche entre l'isr et le code de l'application, mais si c'est déjà la couche intermiédiaire ok

Les listeners est un choix d'organisation pour le code.
L'isr s'occupe de lire/écrire dans le ring buffer, puis on revient en mode normal, puis l'application possède des grands buffers pour s'occuper des débordements.
L'application décide quand appeler les listeners, les listeners vont piocher dans les grands buffers.


======================
   Practical Guide
======================

* Installing the tools

sudo apt install qemu-system-arm							# Qemu to execute programs like its on real hardware
sudo apt install gcc-arm-none-eabi binutils-arm-none-eabi	# gcc and binutils to compile, link for a specific target and see what it looks like
sudo apt install gdb-multiarch								# gdb to debug program execution through Qemu port

* Makefile (see Makefile and kernel.ld)

- cross-compile the program
	arm-none-eabi-gcc ou arm-none-eabi-as pour compiler les sources
	arm-none-eabi-ld -T kernel.ld -nostdlib -static <fichiers_o> -o build/kernel.elf
	>> custom linker script kernel.ld is used

- execute Qemu
	qemu-system-arm -M versatileab -cpu cortex-a8 -m "32K" nographic -serial mon:stdio -device loader,file=build/kernel.elf
	>> -gdb tcp::<gdb_port> -S to hook gdb to Qemu port and pause the VM

* Debugging

In a new terminal, gdb-multiarch <kernel>.elf
Then:
	target remote localhost:<gdb_port>
	
Examine a value in memory
	x <register_address> ; x /t 0x101F1038 for UART0 interrupt mask in binary

Print a value in binary
	p /t (1 << 4)
	
* Execution overview

In order:
- exception.s: ldr pc, reset_handler_addr
- reset_handler of startup.s
- _start of main.c


Misc. notes
-------------

bar : Base Address Register
stmfd/ldmfd : store/load multiple
